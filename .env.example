# Server Configuration
PORT=3001
NODE_ENV=production

# Database & Seeding
# Set to "true" if you want to force re-seeding the SQLite database on start
FORCE_SEED=false

# Authentication (BetterAuth)
# BETTER_AUTH_SECRET=your-production-secret-here
# BETTER_AUTH_URL=https://your-domain.up.railway.app
